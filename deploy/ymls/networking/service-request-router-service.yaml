apiVersion: v1
kind: Service
metadata:
  name: service-request-router
  namespace: default
spec:
  selector:
    app: service-request-router
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
